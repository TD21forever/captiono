# 安装和验收

## 构建与加载

1. 在 `products/caption-review` 运行 `npm run build:extension`。
2. 打开 `chrome://extensions` 并启用开发者模式。
3. 选择“加载已解压的扩展程序”。
4. 加载 `products/caption-review/dist/extension`。
5. 更新已有安装时，先在扩展卡片上点击刷新，再刷新已打开的视频页。

## YouTube 验收

1. 打开一个带英文字幕的 YouTube 视频。
2. 不打开 Captiono，等待页面播放器就绪。
3. 页面右侧应自动出现 Captiono 面板；扩展图标只负责收起或展开当前标签页的面板。
4. 面板应直接显示已自动读取的字幕，或短暂显示“自动读取中”。
5. 验证字幕轨切换、句子跳转、整句复制、短语解释、批注和导出。
6. 在同一标签页切换到另一个视频，确认字幕自动换成新视频内容。

## Bilibili 验收

1. 打开一个平台确实提供英文字幕的 Bilibili 视频。
2. 等待播放器就绪后查看自动挂载的 Captiono 面板。
3. 面板应直接显示 Bilibili 原始字幕，并标明字幕语言。
4. 多 P 视频切换分 P 后，应自动读取当前分 P 对应的字幕。
5. 在站内切换视频后，旧字幕不得继续显示为当前字幕。

## 预期失败

- 视频没有任何平台字幕：显示“当前视频没有提供可读取的字幕”。
- 网站不是 YouTube 或 Bilibili：显示当前支持范围，不注入页面脚本。
- 平台页面改版或请求失败：保留手动“重新读取当前字幕”用于恢复。

## 多标签页与页面面板验收

1. 同时打开两个不同的 YouTube/Bilibili 视频，确认每页各有且只有一个面板。
2. 在 A 页滚动字幕、搜索或写一段未保存批注，再切换到 B；B 不得出现 A 的瞬时状态。
3. 切回 A，滚动位置和草稿应保留；收起再展开也不得重置。
4. 扩展图标只切换当前标签页；面板不得挤压宿主页，也不得产生横向滚动。

扩展不申请音频捕获权限，也不会提供字幕文件导入入口。
